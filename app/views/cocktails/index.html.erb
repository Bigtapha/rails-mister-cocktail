<div class="container">
  <h1>All cocktails</h1>

<div class = >
  <% @cocktails.each do |cocktail| %>
    <div class=>
      <h3><%= link_to cocktail.name, cocktail_path(cocktail)%></h3>
      <%= cl_image_tag cocktail.image, height: 150, width: 150, crop: :fill %>
    </div>
  <% end %>
  </div>

  <br>
  <%= link_to 'Add new cocktail', new_cocktail_path %>
</div>

